# E086: redundant delete statement

The following delete statement is redundant:

    let x = 3;
    delete x;
    console.log(x);

To fix this warning, remove the delete statement:

    let x = 3;
    console.log(x);
